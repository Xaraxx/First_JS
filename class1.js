console.log('Hola mundo JS!')

var name = 'Sacha', last_name = 'Ortega';
var upper = name.toUpperCase();
var lower = last_name.toLowerCase();
var firstLetter = name.charAt(0);
var howManyLetters = name.length

var allName = name +' '+ last_name
var allName2 = `${name} ${last_name}`
var str = name.charAt(1) + last_name.charAt(2)

var str2 = name.charAt(howManyLetters - 1)
var str3 = name.substr(1, 2)